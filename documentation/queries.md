# Complex SQL Queries Reference

This document mirrors the categorized SQL in `database/schema.sql` and provides quick copy/paste blocks for reporting and viva/demo usage.

See `database/schema.sql` for the executable schema + query suite.
